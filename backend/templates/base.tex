\documentclass[letterpaper,{{theme.designSystem.baseFontSize}}]{article}

% ── Font & Encoding ───────────────────────────────────────────────────────────
% cmap ensures proper PDF character mapping (must be loaded before fontenc)
% T1 encoding gives proper glyph coverage (ligatures, accented chars, etc.)
% utf8 input allows unicode characters in source
% lmodern provides high-quality Computer Modern fonts in T1 encoding
\usepackage{cmap}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}

% ── Core Packages ─────────────────────────────────────────────────────────────
\usepackage[
  top=0.35in,
  bottom=0.35in,
  left=0.6in,
  right=0.6in
]{geometry}
\usepackage{enumitem}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{ragged2e}

% ── Micro-Typography ─────────────────────────────────────────────────────────
% microtype enables character protrusion and font expansion for
% professional-quality text layout (fewer overfull/underfull hboxes)
\usepackage[
  activate={true,nocompatibility},
  tracking=true,
  factor=1100,
  stretch=10,
  shrink=10
]{microtype}

% ── Hyperlinks (ATS-safe configuration) ──────────────────────────────────────
\usepackage[
  hidelinks,
  unicode=true,
  pdfstartview=FitH
]{hyperref}
\urlstyle{same}
\input{glyphtounicode}
\pdfgentounicode=1

% ── Theme Font ────────────────────────────────────────────────────────────────
{{theme.fontPackages}}
{{theme.fontDefault}}

% ── Page Setup ────────────────────────────────────────────────────────────────
\pagestyle{empty}
\raggedbottom
\raggedright
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{2em}
\setlength{\tabcolsep}{0.08in}

% ── Widow / Orphan Prevention ────────────────────────────────────────────────
% Prevent section headers from being stranded at the bottom of a page
% and single lines from being stranded at the top
\widowpenalty=10000
\clubpenalty=10000
\brokenpenalty=10000

% ── Line Spacing ──────────────────────────────────────────────────────────────
\renewcommand{\baselinestretch}{ {{theme.designSystem.lineStretch}} }

% ── Section Headings ─────────────────────────────────────────────────────────
{{theme.sectionSetup}}
\newcommand{\resumesection}[1]{%
  \par\vspace{ {{theme.designSystem.sectionSpacingBefore}} }%
  { {{theme.sectionTitleStyle}} #1\par }%
  \nopagebreak[4]\vspace{ {{theme.designSystem.sectionSpacingAfter}} }%
  {\color{ {{theme.sectionRuleColor}} }\rule{\textwidth}{0.4pt}}\par%
  \nopagebreak[4]\vspace{1pt}%
}

% ── List Formatting (ATS-safe — text-mode bullet, not math-mode) ─────────────
\setlist[itemize]{
  nosep,
  itemsep=0pt,
  topsep=1pt,
  parsep=0pt,
  partopsep=0pt,
  leftmargin=0.18in,
  label={\textbullet}
}

% ── ATS PDF Metadata ─────────────────────────────────────────────────────────
\hypersetup{
  pdfauthor={ {{name}} },
  pdftitle={ {{name}} - Resume },
  pdfsubject={Professional Resume},
}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}

{{> header}}
\vspace{ {{theme.designSystem.headerSpacing}} }

{{> summary}}

{{> skills}}

{{> experience}}

{{> education}}

{{> projects}}

{{> awards}}

{{> certifications}}

{{> dsa}}

{{> extracurricular}}

{{> languages}}

\vfill

\end{document}
