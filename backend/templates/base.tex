\documentclass[letterpaper,{{theme.designSystem.baseFontSize}}]{article}

% ── Core Packages ─────────────────────────────────────────────────────────────
\usepackage[left=0.55in,right=0.55in,top=0.45in,bottom=0.45in]{geometry}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{titlesec}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{latexsym}
\usepackage{ragged2e}

% ── Theme Font ────────────────────────────────────────────────────────────────
{{theme.fontPackages}}
{{theme.fontDefault}}

% ── Page Setup ────────────────────────────────────────────────────────────────
\pagestyle{empty}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}
\urlstyle{same}
\pdfgentounicode=1

% ── Line Spacing / Height ─────────────────────────────────────────────────────
\renewcommand{\baselinestretch}{ {{theme.designSystem.lineStretch}} }

% ── Theme Section Formatting ──────────────────────────────────────────────────
{{theme.sectionFormat}}

% ── Deterministic Spacing System ──────────────────────────────────────────────
\titlespacing*{\section}{0pt}{ {{theme.designSystem.sectionSpacingBefore}} }{ {{theme.designSystem.sectionSpacingAfter}} }

% ── Premium List Formatting ───────────────────────────────────────────────────
\setlist[itemize]{
  itemsep={{theme.designSystem.bulletSpacing}},
  topsep=2pt,
  parsep=0pt,
  partopsep=0pt,
  leftmargin=0.15in,
  label={\small $\bullet$}
}

% ── DOCUMENT BODY ─────────────────────────────────────────────────────────────
\begin{document}

{{> header}}
\vspace{ {{theme.designSystem.headerSpacing}} }

{{#if summary}}
% ── PROFESSIONAL SUMMARY ──────────────────────────────────────────────────────
{{> summary}}

\vspace{6pt}
{{/if}}

{{#if skills.length}}
% ── TECHNICAL SKILLS ──────────────────────────────────────────────────────────
{{> skills}}

\vspace{6pt}
{{/if}}

{{#if experience.length}}
% ── EXPERIENCE ────────────────────────────────────────────────────────────────
{{> experience}}

\vspace{6pt}
{{/if}}

{{#if education.length}}
% ── EDUCATION ─────────────────────────────────────────────────────────────────
{{> education}}

\vspace{6pt}
{{/if}}

{{> projects}}

\vfill

\end{document}
