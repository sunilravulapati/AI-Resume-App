\documentclass[letterpaper,{{theme.designSystem.baseFontSize}}]{article}

% ── Core Packages ─────────────────────────────────────────────────────────────
\usepackage[
top=0.55in,
bottom=0.55in,
left=0.65in,
right=0.65in
]{geometry}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{latexsym}
\usepackage{ragged2e}

% ── Theme Font ────────────────────────────────────────────────────────────────
{{theme.fontPackages}}
{{theme.fontDefault}}

% ── Page Setup ────────────────────────────────────────────────────────────────
\pagestyle{empty}
\raggedbottom
\raggedright
\setlength{\parindent}{0pt}
\setlength{\emergencystretch}{1.5em}
\setlength{\tabcolsep}{0.08in}
\urlstyle{same}
\pdfgentounicode=1

% ── Line Spacing ──────────────────────────────────────────────────────────────
\renewcommand{\baselinestretch}{ {{theme.designSystem.lineStretch}} }

% ── Section headings (custom — avoids titlesec titlerule overlap) ─────────────
{{theme.sectionSetup}}
\newcommand{\resumesection}[1]{%
  \par\vspace{ {{theme.designSystem.sectionSpacingBefore}} }%
  { {{theme.sectionTitleStyle}} #1\par }%
  \nopagebreak\vspace{5pt}%
  {\color{ {{theme.sectionRuleColor}} }\rule{\textwidth}{0.4pt}}\par%
  \nopagebreak\vspace{ {{theme.designSystem.sectionSpacingAfter}} }%
}

% ── List Formatting ───────────────────────────────────────────────────────────
\setlist[itemize]{
  itemsep={{theme.designSystem.bulletSpacing}},
  topsep=3pt,
  parsep=1pt,
  partopsep=0pt,
  leftmargin=0.20in,
  label={\normalsize $\bullet$}
}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}

{{> header}}
\vspace{ {{theme.designSystem.headerSpacing}} }

{{> summary}}

{{> skills}}

{{> experience}}

{{> education}}

{{> projects}}

\vfill

\end{document}
