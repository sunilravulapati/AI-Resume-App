{{#if projects}}

% ── PROJECTS ──────────────────────────────────────────────────────────────────
\resumesection{Projects}
{{#each projects}}
\noindent\textbf{ {{this.title}} }{{#if this.tech}} $|$ {{this.tech}}{{/if}}\hfill{ {{../theme.designSystem.metaSize}} {{this.meta}} }\par\nopagebreak
\begin{itemize}
{{#each this.bullets}}
  \item {{../theme.designSystem.bulletSize}} {{this}}
{{/each}}
\end{itemize}
{{#unless @last}}\vspace{1pt}{{/unless}}
{{/each}}
{{/if}}
