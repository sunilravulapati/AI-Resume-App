{{#if projects}}

% ── PROJECTS ──────────────────────────────────────────────────────────────────
\resumesection{Projects}
{{#each projects}}
\noindent\textbf{{escapeLatex this.title}}{{#if this.tech}} \textbar{} \textit{\small {{escapeLatex this.tech}}}{{/if}}\hfill{ {{../theme.designSystem.metaSize}} {{escapeLatex this.meta}} }\par\nopagebreak
\begin{itemize}
{{#each this.bullets}}
  \item {{../theme.designSystem.bulletSize}} {{escapeLatex this}}
{{/each}}
\end{itemize}
{{#unless @last}}\vspace{1pt}{{/unless}}
{{/each}}
{{/if}}
