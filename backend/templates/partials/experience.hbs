{{#if experience}}
\resumesection{Experience}
{{#each experience}}
\noindent\textbf{{escapeLatex this.company}} \textbar{} \textit{\small {{escapeLatex this.title}}}{{#if this.tech}} \textbar{} \textit{\small {{escapeLatex this.tech}}}{{/if}}{{#if this.location}} \textbar{} \textit{\small {{escapeLatex this.location}}}{{/if}}\hfill{ {{../theme.designSystem.metaSize}} {{escapeLatex this.dates}} }\par\nopagebreak
\begin{itemize}
{{#each this.bullets}}
  \item {{../theme.designSystem.bulletSize}} {{escapeLatex this}}
{{/each}}
\end{itemize}
{{#unless @last}}\vspace{ {{../theme.designSystem.itemSpacing}} }{{/unless}}
{{/each}}
{{/if}}
